// Minimal stub - no direct Swing imports required here

/**
 * Minimal stub for enhanced UI. Extends the basic BehaviorLoggerUI so the
 * application can attempt to use an "enhanced" entry point without requiring
 * JFreeChart. Replace this file with the full enhanced implementation when
 * JFreeChart is available on the classpath.
 */
public class BehaviorLoggerUIEnhanced extends BehaviorLoggerUI {
    public BehaviorLoggerUIEnhanced() {
        super();
        setTitle("Behavior Logger UI (Enhanced - charts disabled)");
        // Keep this stub minimal. The full enhanced UI (with charts)
        // should be restored when JFreeChart is added to the classpath.
    }
}